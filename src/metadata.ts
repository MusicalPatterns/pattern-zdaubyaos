import { SongID, SongMetadata } from '../../../src/songTypes'

const zdaubyaosSongMetadata: SongMetadata = {
    // tslint:disable-next-line:max-line-length
    description: 'fun with the superparticular series & variants, plus stepwiseSongConfig motion within recurring bounds',
    formattedName: 'Zdaubyaos',
    songId: SongID.ZDAUBYAOS,
}

export {
    zdaubyaosSongMetadata,
}
